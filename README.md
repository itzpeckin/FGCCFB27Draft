# FGCCFB27 Draft Room

Upload the full contents of this folder to a new GitHub repository. Keep `index.html`, `css/styles.css`, `js/data.js`, and `js/app.js` in the same folder structure.

Firebase is already configured for your project. Use Realtime Database rules with read/write enabled for the friend draft.

Commissioner PIN: 0515

Open `index.html` from GitHub Pages for the best experience.
